steps:
- label: 'Build ios and darwin'
  command:
    - source ~/.bash_profile
    - ./build.sh -p ios,desktop -i -s release debug
  agents:
    queue: mac_ci_v9
  timeout_in_minutes: 90

- label: 'Build webgl'
  command:
    - ./build.sh -p webgl -i release debug
  agents:
    queue: mac_ci_v9
  timeout_in_minutes: 90

- label: 'Build android and webgl'
  command:
    - ./build.sh -p android -i release debug
  agents:
    queue: android_ci_v1-slam
  timeout_in_minutes: 90
